workflows:
  android_production:
    name: 🚀 Android Production Build
    environment:
      vars:
        FLUTTER_VERSION: "3.32.4"  # تم التحديث لآخر إصدار مستقر
        GRADLE_VERSION: "8.0.2"     # تم تغيير الإصدار إلى 8.0.2
        KOTLIN_VERSION: "1.8.22"    # تم تعديل لتتوافق مع Gradle 8.0
        FIREBASE_API_KEY: $FIREBASE_API_KEY
        FIREBASE_APP_ID: $FIREBASE_APP_ID
        FIREBASE_SERVICE_ACCOUNT_JSON: $FIREBASE_SERVICE_ACCOUNT_JSON
    
    scripts:
      - name: ⬇️ تثبيت Flutter
        script: |
          export FLUTTER_HOME="$HOME/programs/flutter"
          rm -rf $FLUTTER_HOME
          git clone https://github.com/flutter/flutter.git -b $FLUTTER_VERSION $FLUTTER_HOME
          export PATH="$FLUTTER_HOME/bin:$PATH"
          flutter doctor -v

      - name: 🛠️ إعداد Gradle
        script: |
          # إنشاء هيكل Android إذا لم يكن موجوداً
          if [ ! -d "android" ]; then
            flutter create --platforms android .
          fi
          
          # استخدام رابط بديل لـ Gradle
          echo "distributionUrl=https\://services.gradle.org/distributions/gradle-$GRADLE_VERSION-bin.zip" > android/gradle/wrapper/gradle-wrapper.properties
          
          # إنشاء local.properties
          echo "flutter.sdk=$FLUTTER_HOME" > android/local.properties
          echo "sdk.dir=$ANDROID_SDK_ROOT" >> android/local.properties
          
          # منح صلاحيات التنفيذ
          chmod +x android/gradlew

      - name: 🔄 تحديث التبعيات
        script: |
          flutter pub get
          cd android && ./gradlew clean --stacktrace && cd ..

      - name: 🏗️ بناء APK
        script: |
          flutter build apk --release \
            --dart-define=FIREBASE_API_KEY=$FIREBASE_API_KEY \
            --dart-define=FIREBASE_APP_ID=$FIREBASE_APP_ID \
            --verbose

      - name: 🚀 النشر على Firebase
        script: |
          echo "$FIREBASE_SERVICE_ACCOUNT_JSON" > /tmp/firebase_account.json
          firebase appdistribution:distribute \
            build/app/outputs/flutter-apk/app-release.apk \
            --app $FIREBASE_APP_ID \
            --groups "testers" \
            --service-account-json /tmp/firebase_account.json
    
    artifacts:
      - build/app/outputs/**/*.apk
      - android/gradle/wrapper/gradle-wrapper.properties
    cache:
      cache_paths:
        - ~/.gradle/caches
        - ~/.pub-cache
